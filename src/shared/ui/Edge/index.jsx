import React from 'react';
import { Box } from '@mui/material';

const Edge = () => {
    return (
        <Box
            sx={{
                height: 4,
                width: 40,
                borderRadius: 12,
                backgroundColor: '#000',
                mx: 1,
                opacity: 0.15,
            }}
        />
    );
};

export default Edge;
